// log.js
// log config file

module.exports = {
  active: true,

  // if true, owner(s) will a get a copy of DMs the bot receives
  ccDM: true,

  // filename where logs are written 
  logfile: 'log.txt'
}
